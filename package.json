{
  "name": "ballard-truck-finder",
  "version": "0.1.0",
  "private": true,
  "description": "Static Ballard food truck tracker with a manual scraper refresh.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "deploy:check": "npm test && npm run build && node scripts/check-dist.js",
    "scrape": "node scripts/scrape.js",
    "test": "node --test"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "date-fns": "^4.1.0",
    "jsdom": "^29.1.1"
  }
}
